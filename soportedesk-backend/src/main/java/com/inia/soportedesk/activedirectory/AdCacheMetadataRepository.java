package com.inia.soportedesk.activedirectory;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AdCacheMetadataRepository extends JpaRepository<AdCacheMetadata, String> {
}
